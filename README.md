# Startup Instructions

Must start up backend first in venv with 'source venv/bin/activate'
Then run the main.py file to start uvicorn server to handle requests

To start frontend use 'npm run dev' command to bring up the locally hosted website